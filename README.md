## Мой проект - сайт с мозгами на Python

Сайт умеет рекомендовать фильмы или игры по вводу настроения подьзователя.

### Стек
Мы использовали HTML, CSS, Python. Реализован фулстак сайт, с созданием локального сервера и подключением нейросети к сайту.
Использованы библиотеки Flask, transformers, pipeline. 

```
   pip install Flask
   pip install transformers
   pip install pipeline
   pip install torch

